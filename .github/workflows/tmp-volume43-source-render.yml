name: Temporary Volume 43 helper frozen

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary Volume 43 helper is frozen; no repository mutation."
