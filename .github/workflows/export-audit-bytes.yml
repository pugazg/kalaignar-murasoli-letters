name: Export Volume 49 byte chunks

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/export-audit-bytes.yml'

permissions:
  contents: write

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Build byte-sized export chunks
        shell: bash
        run: |
          rm -rf .audit-export
          mkdir -p .audit-export
          tar -czf /tmp/volume49-repo.tar.gz README.md TRANSCRIPTION_GUIDE.md volumes/volume-49
          base64 -w 0 /tmp/volume49-repo.tar.gz | split -b 8000 -d -a 3 - .audit-export/part-
          sha256sum /tmp/volume49-repo.tar.gz > .audit-export/SHA256SUMS
          printf '%s\n' .audit-export/part-* | sed 's#^#- #' > .audit-export/MANIFEST.md
          rm -f .github/workflows/export-audit-bytes.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Export Volume 49 in byte-sized audit chunks"
          git push origin HEAD:main
